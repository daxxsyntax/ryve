pre-commit:
  commands:
    cargo-fmt:
      glob: "*.rs"
      run: rustup run nightly cargo fmt
      stage_fixed: true

pre-push:
  commands:
    cargo-clippy:
      glob: "*.rs"
      run: cargo clippy -- -D warnings
